box: wercker/ruby
build:
  steps:
    - rvm-use:
        version: ruby-2.1.2
    - bundle-install
    - script:
        name: rspec
        code: for D in *; do if [ -d "${D}" ]; then bundle exec rspec "${D}/spec" fi done
